//DISABLE ESLING FOR THE ENTIRE FILE(SINCE ESLINT CONFIUGRED FOR NODE JS - NOT JS CLIENT)
console.log('HELLO FROM THE FRONT END');

//READ THE locations data from the HTML data attribute:
//Reading the data-attribute(data-locations) from the HTML
// const locations = document.getElementById('map').dataset.locations;
const locations = document.getElementById('map').dataset.locations;

console.log(locations);
